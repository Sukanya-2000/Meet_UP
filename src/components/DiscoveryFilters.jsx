export default function DiscoveryFilters({ filters, setFilters, onApply, onClose }) {
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/75 p-4"><div className="glass w-full max-w-md rounded-3xl p-7"><h2 className="text-3xl">Advanced filters</h2><div className="mt-6 grid grid-cols-2 gap-4"><label className="label">Minimum age<input className="input mt-2" type="number" min="18" max="100" value={filters.ageMin} onChange={(e) => setFilters({ ...filters, ageMin: e.target.value })} /></label><label className="label">Maximum age<input className="input mt-2" type="number" min="18" max="100" value={filters.ageMax} onChange={(e) => setFilters({ ...filters, ageMax: e.target.value })} /></label></div><label className="label mt-4">Gender<select className="input mt-2" value={filters.gender} onChange={(e) => setFilters({ ...filters, gender: e.target.value })}><option value="">Everyone</option><option value="woman">Women</option><option value="man">Men</option><option value="non-binary">Non-binary</option><option value="other">Other</option></select></label><label className="mt-5 flex items-center gap-3 text-sm"><input type="checkbox" checked={filters.verifiedOnly} onChange={(e) => setFilters({ ...filters, verifiedOnly: e.target.checked })} /> Verified profiles only</label><button onClick={onApply} className="btn-primary mt-7">Apply filters</button><button onClick={onClose} className="mt-4 w-full text-sm text-white/45">Cancel</button></div></div>;
}
