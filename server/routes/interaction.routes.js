import {Router} from 'express';import{protect}from'../middleware/auth.middleware.js';import asyncHandler from'../utils/asyncHandler.js';import{list,start,update}from'../controllers/call.controller.js';import{answer,start as startGame}from'../controllers/game.controller.js';const router=Router();router.use(protect);router.get('/calls',asyncHandler(list));router.post('/calls',asyncHandler(start));router.put('/calls/:id',asyncHandler(update));router.post('/games',asyncHandler(startGame));router.post('/games/:id/answer',asyncHandler(answer));export default router;
