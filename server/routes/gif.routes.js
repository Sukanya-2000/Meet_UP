import { Router } from 'express'; import { protect } from '../middleware/auth.middleware.js'; import asyncHandler from '../utils/asyncHandler.js'; import { activity, save, search, trending } from '../controllers/gif.controller.js'; const router = Router(); router.use(protect); router.get('/search', asyncHandler(search)); router.get('/trending', asyncHandler(trending)); router.get('/activity', asyncHandler(activity)); router.put('/activity', asyncHandler(save)); export default router;
