import { Router } from 'express'; import { protect } from '../middleware/auth.middleware.js'; import asyncHandler from '../utils/asyncHandler.js'; import { extend, history, rematch, snooze } from '../controllers/lifecycle.controller.js'; const router=Router(); router.use(protect); router.post('/matches/:id/extend',asyncHandler(extend)); router.post('/matches/:id/rematch',asyncHandler(rematch)); router.get('/matches/:id/history',asyncHandler(history)); router.put('/snooze',asyncHandler(snooze)); export default router;
