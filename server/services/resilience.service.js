export const retry=async(fn,{attempts=3,baseDelayMs=100,timeoutMs=5000}={})=>{let error;for(let i=0;i<attempts;i+=1){try{return await Promise.race([fn(),new Promise((_,reject)=>setTimeout(()=>reject(new Error('Operation timed out')),timeoutMs))])}catch(e){error=e;if(i<attempts-1)await new Promise(resolve=>setTimeout(resolve,baseDelayMs*2**i))}}throw error};export class CircuitBreaker{constructor({failureThreshold=5,cooldownMs=30000}={}){this.failureThreshold=failureThreshold;this.cooldownMs=cooldownMs;this.failures=0;this.openedAt=null}async execute(fn){if(this.openedAt&&Date.now()-this.openedAt<this.cooldownMs)throw new Error('Circuit breaker is open');try{const value=await fn();this.failures=0;this.openedAt=null;return value}catch(error){this.failures+=1;if(this.failures>=this.failureThreshold)this.openedAt=Date.now();throw error}}}
