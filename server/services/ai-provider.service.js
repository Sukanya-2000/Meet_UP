class AIProvider{async profile(){throw new Error('Not implemented')}async photo(){throw new Error('Not implemented')}async moderate(){throw new Error('Not implemented')}}
class LocalRulesAI extends AIProvider{async profile({profile,capability}){const missing=['bio','occupation','relationshipIntentions','interests','prompts'].filter(k=>k==='prompts'?!(profile.prompts?.length):!(profile[k]?.length||profile[k]));const suggestions={bio:`Share one specific thing you enjoy about ${profile.city||'your city'} and what kind of connection you value.`,prompts:['What always makes your day better?','A perfect weekend looks like…'],conversationStarters:(profile.interests||[]).slice(0,3).map(i=>`What got you into ${i}?`),interests:['Cooking','Nature','Live music'].filter(i=>!(profile.interests||[]).includes(i)),completion:missing};return{provider:'local-rules',capability,suggestions,explanation:'Suggestions use profile completeness and shared-interest signals; no personal data leaves CyberNest.'}}async photo({metadata={}}){const width=Number(metadata.width)||0,height=Number(metadata.height)||0,size=Number(metadata.size)||0;const quality=Math.min(100,Math.round((width>=800?35:15)+(height>=800?35:15)+(size>=100000?30:10)));return{provider:'local-rules',scores:{quality,lighting:null,blur:null,smile:null,faceVisibility:null,groupPhoto:null},duplicateKey:metadata.hash||null,suggestions:quality<70?['Use a higher-resolution, well-lit photo.']:['Photo resolution looks good.'],requiresVisionProvider:true}}async moderate({text=''}){return{safe:!/(?:hate|threat|scam)/i.test(text),provider:'local-rules'}}}
class ExternalAI extends AIProvider{constructor(name){super();this.name=name}async profile(){const e=new Error(`${this.name} AI provider adapter is not configured`);e.statusCode=503;throw e}async photo(){return this.profile()}async moderate(){return this.profile()}}
export const aiProvider=()=>{const name=process.env.AI_PROVIDER||'local';return name==='local'?new LocalRulesAI():new ExternalAI(name)};export{AIProvider,LocalRulesAI,ExternalAI};
