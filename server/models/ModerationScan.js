import mongoose from 'mongoose';
const schema = new mongoose.Schema({ userId: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true, index: true }, targetType: { type: String, enum: ['image','message','id'], required: true }, targetId: { type: String, required: true }, provider: { type: String, default: 'rules' }, categories: [{ type: String, enum: ['explicit','violence','spam','scam','fake-id'] }], score: { type: Number, min: 0, max: 1, default: 0 }, action: { type: String, enum: ['allow','warn','blur','queue','block'], default: 'allow' }, status: { type: String, enum: ['completed','queued','reviewed'], default: 'completed', index: true }, reviewedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User', default: null } }, { timestamps: true }); schema.index({ status: 1, createdAt: -1 });
export default mongoose.model('ModerationScan', schema);
