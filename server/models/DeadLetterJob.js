import mongoose from'mongoose';const schema=new mongoose.Schema({jobName:{type:String,required:true,index:true},payload:{type:mongoose.Schema.Types.Mixed,default:{}},error:{type:String,required:true},attempts:{type:Number,default:0},status:{type:String,enum:['pending','retried','discarded'],default:'pending',index:true}},{timestamps:true});export default mongoose.model('DeadLetterJob',schema);
