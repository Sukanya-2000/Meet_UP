import mongoose from 'mongoose';
const schema = new mongoose.Schema({ matchId: { type: mongoose.Schema.Types.ObjectId, ref: 'Match', required: true, index: true }, callerId: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true }, receiverId: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true }, type: { type: String, enum: ['voice','video'], required: true }, provider: { type: String, default: 'dummy' }, roomId: { type: String, required: true, unique: true }, status: { type: String, enum: ['ringing','connected','ended','declined','missed','failed'], default: 'ringing', index: true }, startedAt: { type: Date, default: Date.now }, connectedAt: { type: Date, default: null }, endedAt: { type: Date, default: null }, durationSeconds: { type: Number, min: 0, default: 0 } }, { timestamps: true });
export default mongoose.model('CallSession', schema);
